{
  "$schema": "../schema/fixtures.schema.json",
  "response": [
    {
      "slug": "feature-a",
      "value": true,
      "reason": "The flag is enabled for this environment."
    },
    {
      "slug": "feature-b",
      "value": false,
      "reason": "The flag is disabled for this environment."
    }
  ],
  "cases": [
    {
      "description": "enabled when evaluating a flag that returns a value of true",
      "configuration": {
        "slug": "feature-a",
        "defaultValue": false
      },
      "expected": {
        "value": true,
        "reason": "The flag is enabled for this environment."
      }
    },
    {
      "description": "disabled when evaluating a flag that returns a value of false",
      "configuration": {
        "slug": "feature-b",
        "defaultValue": false
      },
      "expected": {
        "value": false,
        "reason": "The flag is disabled for this environment."
      }
    },
    {
      "description": "enabled when evaluating a flag that does not exist and default value is true",
      "configuration": {
        "slug": "feature-not-exist",
        "defaultValue": true
      },
      "expected": {
        "value": true,
        "errorCode": "FLAG_NOT_FOUND"
      }
    },
    {
      "description": "disabled when evaluating a flag that does not exist and default value is false",
      "configuration": {
        "slug": "feature-not-exist",
        "defaultValue": false
      },
      "expected": {
        "value": false,
        "errorCode": "FLAG_NOT_FOUND"
      }
    },
    {
      "description": "disabled when default value is false and slug is not a slug",
      "configuration": {
        "slug": "Feature A",
        "defaultValue": false
      },
      "expected": {
        "value": false,
        "errorCode": "FLAG_NOT_FOUND"
      }
    },
    {
      "description": "evaluation is case insensitive for slug",
      "configuration": {
        "slug": "FEATURE-A",
        "defaultValue": false
      },
      "expected": {
        "value": true,
        "reason": "The flag is enabled for this environment."
      }
    }
  ]
}
