{
  "$schema": "../schema/fixtures.schema.json",
  "response": [
    {
      "slug": "feature-a",
      "isEnabled": true,
      "segments": [],
      "clientRolloutPercentage": 100
    },
    {
      "slug": "feature-b",
      "isEnabled": true,
      "evaluationKey": "evaluation-key",
      "clientRolloutPercentage": 100
    },
    {
      "slug": "feature-c",
      "isEnabled": true,
      "evaluationKey": "evaluation-key",
      "segments": []
    }
  ],
  "cases": [
    {
      "description": "returns error when evaluating a toggle that returns a value of true but missing evaluationKey",
      "configuration": {
        "slug": "feature-a",
        "defaultValue": false
      },
      "expected": {
        "value": false,
        "errorCode": "PARSE_ERROR"
      }
    },
    {
      "description": "returns error when evaluating a toggle that returns a value of true but missing segments",
      "configuration": {
        "slug": "feature-b",
        "defaultValue": false
      },
      "expected": {
        "value": false,
        "errorCode": "PARSE_ERROR"
      }
    },
    {
      "description": "returns error when evaluating a toggle that returns a value of true but missing clientRolloutPercentage",
      "configuration": {
        "slug": "feature-c",
        "defaultValue": false
      },
      "expected": {
        "value": false,
        "errorCode": "PARSE_ERROR"
      }
    }
  ]
}