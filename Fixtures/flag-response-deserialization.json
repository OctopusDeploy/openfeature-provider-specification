{
  "$schema": "../schema/fixtures.schema.json",
  "response": [
    {
      "slug": "feature-with-extra-fields",
      "value": true,
      "reason": "The flag is enabled for this environment.",
      "cats": "dogs",
      "fish": 123,
      "pianos": { "nested": "value" }
    },
    {
      "slug": "feature-with-extra-field-in-condition",
      "evaluationKey": "evaluation-key",
      "rules": [
        {
          "name": "Client-side targeting",
          "conditions": [
            {
              "type": "context-attribute-is-one-of",
              "key": "license-type",
              "values": [
                "free"
              ],
              "more": "data"
            }
          ]
        }
      ]
    },
    {
      "slug": "feature-with-extra-field-in-rule",
      "evaluationKey": "evaluation-key",
      "rules": [
        {
          "name": "Client-side targeting",
          "conditions": [
            {
              "type": "context-attribute-is-one-of",
              "key": "license-type",
              "values": [
                "free"
              ]
            }
          ],
          "cats": "dogs"
        }
      ]
    },
    {
      "slug": "feature-with-extra-field-when-deferred",
      "evaluationKey": "evaluation-key",
      "rules": [
        {
          "name": "Client-side targeting",
          "conditions": [
            {
              "type": "context-attribute-is-one-of",
              "key": "license-type",
              "values": [
                "free"
              ]
            }
          ]
        }
      ],
      "pianos": { "nested": "value" }
    }
  ],
  "cases": [
    {
      "description": "should deserialize and evaluate correctly for a flag with extra fields",
      "configuration": {
        "slug": "feature-with-extra-fields",
        "defaultValue": false
      },
      "expected": {
        "value": true,
        "reason": "The flag is enabled for this environment."
      }
    },
    {
      "description": "should deserialize and evaluate correctly for a flag with an extra field in a condition",
      "configuration": {
        "slug": "feature-with-extra-field-in-condition",
        "defaultValue": false,
        "context": {
          "license-type": "free"
        }
      },
      "expected": {
        "value": true,
        "reason": "Matched rule 'Client-side targeting'."
      }
    },
    {
      "description": "should deserialize and evaluate correctly for a flag with an extra field in a rule",
      "configuration": {
        "slug": "feature-with-extra-field-in-rule",
        "defaultValue": false,
        "context": {
          "license-type": "free"
        }
      },
      "expected": {
        "value": true,
        "reason": "Matched rule 'Client-side targeting'."
      }
    },
    {
      "description": "should deserialize and evaluate correctly for a flag with an extra field alongside its rules",
      "configuration": {
        "slug": "feature-with-extra-field-when-deferred",
        "defaultValue": false,
        "context": {
          "license-type": "free"
        }
      },
      "expected": {
        "value": true,
        "reason": "Matched rule 'Client-side targeting'."
      }
    }
  ]
}
